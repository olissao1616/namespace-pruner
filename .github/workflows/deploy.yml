name: Deploy

# Deployment is managed by ArgoCD.
# This workflow is a placeholder — update it when the GitOps manifests
# repo is ready and ArgoCD app definitions are in place.
#
# Planned flow:
#   1. Release workflow builds and pushes image to GHCR (already done)
#   2. This workflow opens a PR in the GitOps repo updating the image tag
#   3. ArgoCD detects the change and deploys to each cluster
#
# TODO: implement when GitOps repo is available.

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: ArgoCD deployment not yet configured
        run: |
          echo "Deployment is managed by ArgoCD."
          echo "See README for deployment instructions."
          echo "TODO: wire up GitOps repo image tag update here."
