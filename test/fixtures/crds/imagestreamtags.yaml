apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: imagestreamtags.image.openshift.io
spec:
  group: image.openshift.io
  names:
    kind: ImageStreamTag
    listKind: ImageStreamTagList
    plural: imagestreamtags
    singular: imagestreamtag
    shortNames:
      - istag
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            image:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            tag:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            generation:
              type: integer
              format: int64
            conditions:
              type: array
              items:
                type: object
                x-kubernetes-preserve-unknown-fields: true
            lookupPolicy:
              type: object
              x-kubernetes-preserve-unknown-fields: true
